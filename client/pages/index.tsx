import React from 'react';
import App from 'next/app';
import Layout from '../components/layout/layout';

export default class BookShop extends App {
    render() {
        return (
            <Layout>
                <></>
            </Layout>
        );
    }
}
